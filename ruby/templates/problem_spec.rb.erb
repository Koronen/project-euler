require 'spec_helper'

require 'project_euler/problem_<%= number %>'

module ProjectEuler
  describe Problem<%= number %> do
    it 'calculates helper_function_value correctly' do
      Problem<%= number %>.helper_function_value.must_equal 0
    end
  end
end
