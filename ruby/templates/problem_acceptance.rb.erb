require 'spec_helper'

require 'project_euler/problem_<%= number %>'

describe ProjectEuler::Problem<%= number %> do
  it 'gives the correct answer' do
    ProjectEuler::Problem<%= number %>.answer.must_equal 0
  end
end
