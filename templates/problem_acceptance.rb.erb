require 'spec_helper'

require 'project_euler/problem_<%= number %>'

module ProjectEuler
  describe Problem<%= number %> do
    it 'gives the correct answer' do
      Problem<%= number %>.answer.must_equal 0
    end
  end
end
